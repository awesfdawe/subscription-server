[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["proxy-schemas"]

[tool.uv.sources]
proxy-schemas = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
