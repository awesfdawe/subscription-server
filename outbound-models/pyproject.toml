[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "outbound-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "msgspec[yaml]>=0.21.1",
]

[tool.ruff]
line-length = 120

[dependency-groups]
dev = [
    "hypothesis>=6.153.0",
    "pytest>=9.0.3",
    "ruff>=0.15.15",
    "ty>=0.0.40",
]
